<Window x:Class="ExcelLab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelLab"
        xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
        xmlns:Views="clr-namespace:ExcelLab.Views"
        mc:Ignorable="d"
        Title="ExcelLab" Height="400" Width="800" MinHeight="400" MinWidth="600">
    <Window.DataContext>
        <local:TableViewModel />
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="local:TableViewModel">
            <Views:Menu/>
        </DataTemplate>
        <local:CellContentConverter x:Key="CellContentConverter"/>
    </Window.Resources>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top">
                <Menu>
                    <MenuItem Header="_File">
                        <MenuItem Header="New" IsCheckable="False"/>
                    </MenuItem>
                </Menu>
                <Border Padding="15">
                    <Border.Background>
                    <SolidColorBrush Color="SeaGreen"/>
                </Border.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="3*" />
                        </Grid.ColumnDefinitions>
                        <Label Content="{Binding LatestSyntaxError}" FontWeight="Bold" FontSize="18"/>
                        <TextBox Height="24" FontSize="14" VerticalAlignment="Center" Grid.Column="1"
                                 Text="{Binding CurrentCell.Content, UpdateSourceTrigger=PropertyChanged}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="LostFocus">
                                    <i:InvokeCommandAction PassEventArgsToCommand="True"
                                                           Command="{Binding CellEditEndedCommand}" />
                                </i:EventTrigger>
                                <i:EventTrigger EventName="GotFocus">
                                    <i:InvokeCommandAction PassEventArgsToCommand="True"
                                                           Command="{Binding CellEditBeganCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBox>
                    </Grid>
                </Border>
            </StackPanel>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <DataGrid RowHeaderWidth="25" LoadingRow="TableDataGrid_OnLoadingRow" RowHeight="30" SelectionMode="Single" SelectionUnit="Cell" CanUserAddRows="False" CanUserSortColumns="False"  CanUserReorderColumns="False" AutoGenerateColumns="False" ItemsSource="{Binding Table.Rows}">
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="FontSize" Value="12"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.RowHeaderStyle>
                        <Style TargetType="DataGridRowHeader">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10 0 0 0"/>
                            <Setter Property="MinHeight" Value="20"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="LightGray"></Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowHeaderStyle>
                    <DataGrid.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                                         Color="Transparent" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"
                                         Color="Black" />
                    </DataGrid.Resources>
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectedCellsChanged">
                            <i:InvokeCommandAction PassEventArgsToCommand="True" 
                                                    Command="{Binding SelectCellCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="Loaded">
                            <i:InvokeCommandAction PassEventArgsToCommand="True"
                                                   Command="{Binding DataGridLoadedCommand}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="BeginningEdit">
                            <i:InvokeCommandAction PassEventArgsToCommand="True"
                                                   Command="{Binding CellEditBeganCommand}" />
                        </i:EventTrigger>
                        <i:EventTrigger EventName="CellEditEnding">
                            <i:InvokeCommandAction PassEventArgsToCommand="True"
                                                   Command="{Binding CellEditEndedCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </DataGrid>
            </ScrollViewer>
        </DockPanel>
    
</Window>
